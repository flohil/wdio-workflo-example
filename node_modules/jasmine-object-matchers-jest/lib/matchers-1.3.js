'use strict';

var objectMatchers = require('./objectMatchers');

module.exports = {
  toContainKeys: function toContainKeys(expected) {
    var _objectMatchers$toContainKeys = objectMatchers.toContainKeys(this.actual, expected);

    var pass = _objectMatchers$toContainKeys.pass;
    var message = _objectMatchers$toContainKeys.message;

    this.message = function () {
      return message;
    };
    return pass;
  },
  toContainValues: function toContainValues(expected) {
    var _objectMatchers$toContainValues = objectMatchers.toContainValues(this.actual, expected);

    var pass = _objectMatchers$toContainValues.pass;
    var message = _objectMatchers$toContainValues.message;

    this.message = function () {
      return message;
    };
    return pass;
  } };